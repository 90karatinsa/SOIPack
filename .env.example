# SOIPack API authentication token
SOIPACK_API_TOKEN=degiştir-beni

# Host port published by docker compose (optional)
PORT=3000

# Optional CA bundle used by scripts/verify-healthcheck.js and Docker healthchecks.
# Leave undefined or blank to fall back to the system certificate store.
# SOIPACK_HEALTHCHECK_CA_PATH=/run/secrets/soipack-ca.crt
